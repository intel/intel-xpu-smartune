openvino>=2025.0.0
openvino-tokenizers>=2025.0.0
nncf>=2.14.0
git+https://github.com/huggingface/optimum-intel.git
# Fix dependency conflicts when using git version of optimum-intel
# Git optimum-intel requires safetensors<0.8.0, so we need to downgrade diffusers
safetensors<0.8.0
diffusers<0.38.0,>=0.22.0
# Note: If optimum-onnx is installed, it may conflict with optimum>=2.2.0
# This warning can be ignored if optimum-onnx is not actively used
pandas>=2.2.0
psutil>=7.0.0
onnxscript
pillow
torchvision
datasets
modelscope>=1.22.0