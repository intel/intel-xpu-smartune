This folder is reserved for monitoring helper scripts.

Reference tools:
- iGPU (i915): `intel_gpu_top` (from `intel-gpu-tools`)
- iGPU (Xe): `qmassa`

The Python monitor will use these tools if they are installed and runnable.
